---
- name: Judges whether this Mac is remote control by Vagrant.
  stat:
    path: /vagrant
  register: vagrant

- name: Remove startup
  shell: osascript -e 'tell application "System Events" to delete login item "setup"'
  when: not vagrant.stat.exists

- name: Locking sudo
  lineinfile:
    dest: /etc/sudoers
    regexp: '{{ ansible_env.USER }} ALL=\(ALL\) NOPASSWD: ALL'
    line: ''
    validate: visudo -cf %s
  become: yes

# - include: ../../common/handlers/reboot.yml
