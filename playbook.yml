---
- name: Setup dev environment for macOS.
  hosts: all
  vars_prompt:
    - name: computername
      prompt: Please set the local host name of this Mac.
      default: my-mac
      private: no
  roles:
    - common
    - brew
    - finish
